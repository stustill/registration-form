import * as React from "react";
import * as ReactDOM from "react-dom";

import { Card } from "./components/Card";

ReactDOM.render(
    <Card title="TypeScript" description="Welcome to using TypeScript with React." />,
    document.getElementById("example")
);